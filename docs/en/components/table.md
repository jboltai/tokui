# Table

The table family (`table` / `thead` / `tbody` / `tr` / `tcol`, plus the `desc` descriptions list and pagination) has been consolidated into the Data Display docs.

→ See [Data Display · Table](./data#table-table)
